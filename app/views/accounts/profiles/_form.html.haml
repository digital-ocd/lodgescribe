- if @profile.errors.any?
  .alert.alert-block.alert-danger
    %a.close{"data-dismiss" => "alert"} ×
    %h4.alert-heading Oh Snap!
    We found #{pluralize(@user.errors.count, "problem")} with the form:

= f.input :birth_date,
    :order => [:month, :day, :year],
    :prompt => {:day => 'Choose day', :month => 'Choose month', :year => 'Choose year'},
    :end_year => Time.now.year, :end_year => Time.now.year - 100,
    :start_year => Time.now.year - 18
= f.input :birth_city
